# Porta padrão onde o site vai rodar
server.port=8080

# Conexão com o PostgreSQL Local
spring.datasource.url=jdbc:postgresql://localhost:5432/StockVision
spring.datasource.username=postgres
spring.datasource.password=32137758

# Configurações do Hibernate / JPA para criar e atualizar as tabelas automaticamente
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true